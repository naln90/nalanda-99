# 02 环境配置说明

## 1. 推荐环境

| 类型 | 推荐版本 | 说明 |
|---|---|---|
| 操作系统 | Windows 10/11 | 赛事组常见环境，脚本按 PowerShell 编写 |
| Python | 3.11.x | 后端 FastAPI 和测试使用 |
| Node.js | 20.19+、22.12+ 或 24+ | Vite 8 对 Node 版本有要求 |
| npm | 10+ | 使用 `npm.cmd ci` 按锁文件安装 |
| 浏览器 | Chrome / Edge | 打开前端页面 |

## 2. 后端依赖

位置：

```text
01_source_main_demo/fraud-pet-demo/backend/requirements.txt
```

主要依赖：

- fastapi==0.115.6
- uvicorn==0.32.1
- SQLAlchemy==2.0.36
- pydantic>=2.0
- pytest==8.3.4
- httpx==0.28.1

后端默认数据库：

```text
01_source_main_demo/fraud-pet-demo/backend/data/demo.sqlite3
```

如果该数据库不存在，后端启动时会自动创建并写入种子数据。

## 3. 前端依赖

位置：

```text
01_source_main_demo/fraud-pet-demo/package.json
01_source_main_demo/fraud-pet-demo/package-lock.json
```

主要依赖：

- React
- Vite
- TypeScript
- Tailwind CSS
- Zustand
- ECharts
- lucide-react

安装方式：

```powershell
cd .\01_source_main_demo\fraud-pet-demo
npm.cmd ci
```

## 4. 端口和环境变量

后端：

```text
http://127.0.0.1:8000
```

前端：

```text
http://127.0.0.1:5173
```

前端默认 API 地址：

```text
http://127.0.0.1:8000/api
```

如需修改前端 API 地址，可设置：

```powershell
$env:VITE_API_BASE_URL="http://127.0.0.1:8000/api"
```

## 5. 无网络说明

系统业务能力默认离线可运行，包括：

- 演示账号登录；
- 首次测评；
- 宠物领取；
- 推荐训练；
- 训练结算；
- 可疑信息判断；
- 成长榜；
- 风险要素抽取、诈骗类型识别、风险评分和建议。

首次安装 npm 和 Python 依赖时需要网络，除非赛事组电脑已经提前缓存依赖。

## 6. 旧版 Streamlit 资料说明

`02_materials_and_outputs/outputs` 中保留旧版 Streamlit 工程、数据、测试报告和答辩材料，主要作为数据资产和工程演进证据。

本次建议赛事组运行 `01_source_main_demo/fraud-pet-demo`，不要以旧版 Streamlit 作为主验收入口。
