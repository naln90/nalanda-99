# 「诈醒学集 / 小盾灵」反诈 AI 学习训练平台 — 全面深度剖析报告

> 剖析对象：`01_source_main_demo/fraud-pet-demo`（React + TypeScript + Vite 前端 / FastAPI + SQLAlchemy + SQLite 后端）
> 剖析方法：逐文件静态阅读前后端全部源码，按六大维度逐项核验，所有结论均附 **真实文件:行号 / 模块名 / 函数名** 证据。
> 剖析定位：面向「启智杯」参赛作品评审的工程质量与业务闭环核查，同时为后续迭代给出可落地方案。

---

## 〇、整体评价（先给结论）

**这个项目"门面"与"主链路"是扎实的**：分层方向正确（`router / layout / pages / components / store / api`），UI 基础组件质量高，无障碍（skip-link、`aria-*`、loading 锁）超出 Demo 水准，**没有 XSS 注入面**（`dangerouslySetInnerHTML` 零命中），隐私匿名化（`U-2408**`、不采集真实身份）与合规免责意识到位。主演示链路（测评→宠物→训练→结算→排行、学习目标→任务包→工作台→成果→集市复用、可疑信息判断→宠物成长）基本真实闭环且数据落库。

**但存在三类必须正视的硬伤，直接关系评审两个核心卖点（"业务闭环""AI 原生"）是否成立：**

1. **零安全基座**：无鉴权（token 不校验、ownerId 客户端可伪造、校方账户空密码哈希、admin 端点裸写）。这是阻断性缺陷，排在最前。
2. **架构一致性缺失**：2553 行巨型 `main.py`、v1/legacy 双实现、三重 schema 管理机制并存、async 路由持有同步 Session。拖慢演进且暗藏并发 Bug。
3. **业务"名不副实"的断点**：三套成长/能量计数器互不打通、V3 盾能贡献默认不可达、`GrowthRule` 后台写了不生效、"AI 初审/陪伴/任务包"默认实为规则引擎。

**综合评分（6 维 / 10 分）：** 架构 5.5 · 功能完整性 6.5 · 代码质量 6.0 · 数据流与状态管理 5.0 · 错误处理与健壮性 5.0 · 安全性与可扩展性 4.0。

**结论**：当前已达到文档自评的"作品级"，**距"产品级"主要缺口在：安全基座、状态管理、统一成长/能量体系、配置真实生效、测试覆盖**。以下逐项展开。

---

## 一、架构设计

### 1.1 当前问题（附代码位置）

- **【严重】巨型 `main.py` 反模式（2553 行，路由/业务/数据三层全部内联）**
  `create_app()`（`app/main.py:388`）内部用 `@app.get/@app.post` 把全部路由定义为嵌套函数：`submit_training`(:926)、`risk_analyze`(:1029)、`scenario_reply`(:1758)、`v1_scenario_finish`(:2375) 等同体。单一函数不可单测、不可复用、不可按模块裁剪，与已采用的"工厂函数返回 `APIRouter`"分层（`v3_routes.py`、`learning_market.py`）自相矛盾。

- **【严重】三重 Schema 管理机制并存，Alembic 被架空**
  `create_app` 中 `Base.metadata.create_all(engine)`（main.py:391）开机建表；又手工 `ALTER TABLE`（`_migrate_pets_table`:344、`_migrate_v3_columns`:357，用 f-string 拼 DDL）；同时存在 `migrations/versions/3b3c8a7b10d2_*`、`04302bbf2b3d_*` 与 `migrations/env.py`（target_metadata=Base.metadata）。但 `create_app` 从不执行 `alembic upgrade`，导致 `migrations/` 成为**孤儿迁移**，长期必产生 schema 漂移。

- **【中等】Fat Route：路由层直接写业务与多张表**
  `v1_scenario_finish`(main.py:~2375) 在路由内联调用 `generate_review_rule`、`schedule_retrain`、`compute_ability_profile` 并**直接写 `ReviewReport/AbilitySnapshot/AbilityEvent/RetrainTask` 四张表**；`submit_training`(:926-1027) 同样在路由里算分、写 `TrainingRecord`、调度 `RetrainTask`。与已存在的 service 层（`review_engine.py`、`retrain_scheduler.py` 等）割裂。

- **【中等】共享全局可变状态**
  `ai_service.py:50-55` 的 `MODEL_NAME/LLM_API_KEY/LLM_BASE_URL/PROMPT_VERSIONS` 均为模块级全局，运行时不可热切换；`emergency_stop_loss.py:88` 用 `item["completed"]=False` **改写模块级常量 `STOP_LOSS_CHECKLIST` 的元素**（运行时污染，见维度五 R3）。

- **【中等】平行/重复端点（历史迭代残留）**
  能力画像 `/api/assessment/ability-profile`(:1351) 与 `/api/v1/assessment/ability-profile`(:716)；任务包 `/api/task-package/*`(:1376) 与 `/api/v1/task-package/*`(:1456)；情景训练 `/api/training/scenario/*`(:1701) 与 `/api/v1/training/scenario/*`(:2203，`ai_enabled=False` 纯规则)；风险分析 `/api/risk/analyze`(:1029) 与 `/api/risk/analyze-ai`(:1927) 共 5 组双实现。

- **【中等】三套成长/能量数据模型并存（跨层架构裂缝）**
  `Pet.growth_value`（宠物路线）、`LearningPlan.shield_energy`（集市路线）、`EnergyLedger`（校方/主题路线）三者互不调用、口径分裂（详见维度二 B1、维度四）。

### 1.2 改进方案

1. 将所有路由抽到独立 `routes/` 模块（`routes/assessment.py`、`routes/scenario.py`、`routes/training.py`…），`create_app` 仅做装配；业务编排下沉到 `services/*`。
2. **删除 `create_all` 与手工 ALTER**，统一走 Alembic（`alembic upgrade head`），`create_app` 仅做 seed。
3. 配置经 DI 注入（`pydantic-settings` + `@lru_cache`），不可变数据用 `tuple` / `MappingProxyType` 防误改。
4. 统一对外接口为 `v1`，旧版加 Deprecation 头或废弃；合并三套成长/能量为单一 `energy_service.award_energy` 账本（见维度四）。
5. 引入 `tenant_id` 维度（多学校隔离），为扩展预留。

---

## 二、功能完整性

### 2.1 逐模块核查表（功能是否真正闭环）

| # | 声称模块 | 后端端点 | 前端调用 | 状态 | 关键证据 |
|---|----------|----------|----------|------|----------|
| 1 | 演示账号登录 | `POST /auth/demo-login` main.py:437 | LoginPage→store.login | ✅ | ownerId 匿名；**token 服务端不校验** |
| 2 | 首次测评 | v1 `POST /v1/assessment/sessions` main.py:570 | AssessmentPage(v1) | ✅ | 分层随机 `select_questions`(assessment_service.py:164) 真生效 |
| 3 | 宠物领取/成长 | `POST /pets/claim` main.py:830 | PetSelectPage→store.adoptPet | ✅ | growth 仅在训练/风险判断时增长 |
| 4 | 训练/结算+能量 | `POST /training/submit` main.py:926 | SettlementPage→store.submitTraining | ✅ | 幂等防护 `awarded_training_today`(main.py:942) |
| 5 | 可疑信息判断 | `POST /risk/analyze` main.py:1029 | SuspiciousCheckPage | ✅ | 真落库 `SuspiciousCheck`(main.py:1047) |
| 6 | 成长榜 | `GET /ranking` main.py:1064 | RankingPage | ✅ | 仅读 `Pet.growth_value` |
| 7 | 学习目标发布 | `POST /learning/goals` learning_market.py:753 | LearningGoalPage | ✅ | 规则校验 `_validate_goal` |
| 8 | 任务包生成/微调 | 双份 legacy/v1 (main.py:1376+) | TaskPackagePage(v1) | ⚠️ 部分 | `TaskPackageItem.taskRef` 不关联真实训练 |
| 9 | 学习工作台 | `GET /learning/dashboard` learning_market.py:782 | LearningWorkspacePage | ✅ | 小盾灵陪伴为**规则匹配**(learning_market.py:941) |
| 10 | 成果工坊 V1/V2+初审 | `POST /artifacts|/versions|/publish` learning_market.py:968/989/1047 | ArtifactStudioPage | ✅版本落库 / ⚠️初审名不副实 | `_artifact_review` 为规则打分(learning_market.py:583) |
| 11 | 学习集市复用 | `GET /market`、`POST /market/{id}/reuse` learning_market.py:1138/1169 | LearningMarketPage | ✅ | 复用创建**新** goal+plan，不改动原作者 |
| 12 | 守护活动 | 集市 `_evaluate_unlocks` / V3 `contribute` activity_service.py:68 | CampusActivitiesPage / SchoolActivityPage | ⚠️ 两条路并存 | 解锁走"任务计数"；V3 盾能贡献需 `EnergyLedger` |
| 13 | 反诈情景训练 | v1 `start/reply/finish` main.py:2203/2265/2375 | ScenarioTrainingPage | ✅ | v1 `ai_enabled=False` 纯规则；复盘存 `ReviewReport` |
| 14 | 紧急止损 | `GET/POST /emergency-stop-loss*` main.py:1938/1944 | EmergencyStopLossPage | ✅ | 纯规则清单 |
| 15 | 校方端主题/活动/审核 | v3_routes.py:159/251/266/350 | School*Page | ⚠️ 可达但默认 inert | seed 不发布主题 → `get_active_theme` 返回 None |

### 2.2 缺失 / 不完善逻辑 + 边界条件（核心断点）

- **【严重】三套成长/能量计数器互不打通（B1）**
  - `Pet.growth_value` 仅训练/风险判断累加；`LearningPlan.shield_energy` 学习集市完成时才 `+=`（learning_market.py:864，从不调 `energy_service`）；`EnergyLedger` 仅校方主题路线写入。
  - 排行榜只读 `Pet.growth_value`，V3 盾能页只读 `EnergyLedger`。**结果：只走"AI 学习集市"主演示路线的用户，排行榜不动、EnergyLedger 恒为 0**——所谓"统一盾能体系"并不统一。

- **【严重】V3 盾能贡献 / 共建解锁默认不可达（B2）**
  `seed.py:518` 不发布任何 Theme、不创建 CampusActivity（活动由 `_ensure_activity_seeds` 运行时插入，状态 `building`）。→ `get_active_theme` 默认 None → 学生无法加入主题 → `award_energy` 永不触发 → `EnergyLedger` 为空 → `POST /activities/{id}/contribute` 因余额 0 **必然失败**。主演示里"守护活动—盾能贡献"这一卖点是断的。

- **【中】"AI 初审/陪伴/任务包文案"实为规则，对外宣称 AI（B3/B4）**
  `_artifact_review`(learning_market.py:583) 按长度+关键词给 0–96 分，代码自标 `source:"AI成果初审（规则降级可用）"`；小盾灵陪伴(learning_market.py:924)、任务包文案(`generate_plan_rule` 返回空 motivationText，main.py:1407 写死) 默认均无 LLM。`ai_service.is_llm_available()`(ai_service.py:58) 默认 false，全走规则降级。"AI 原生"在交付态实为"规则引擎 + 结构化输出"。

- **【中】`GrowthRule` 表是摆设（B6）**
  表由 seed 写入并可经 `GET/PUT /api/admin/rules`(main.py:1244-1261) 读写，但**无任何成长计算代码读取它**；真实逻辑全在 `rules.py` 常量（`DAILY_MAX_GROWTH=300` 等）。管理员改规则对实际发放**毫无影响**。

- **【中】`get_session_questions` 全量返回但属死代码（B5）**
  `assessment_service.py:425-454` 确为 `return all_questions`，但 grep 显示**无处被调用**；v1 路径真正选题走 `select_questions`（分层随机真实生效）。该函数是误导性遗留死代码。

- **【边界】测评成长值从不入宠物（B7）**
  `/api/assessment/submit`(main.py:505) 与 `/v1/assessment/sessions/complete`(main.py:684) 都返回 `growthAwarded`(30/20)，但**均不调 `apply_growth`**，仅前端展示。

- **【边界】风险判断需先领宠才计成长（流程③）**
  `risk_analyze`(main.py:1043) 条件 `if pet and ...`，"先判断后领宠"顺序下成长值丢失；且不写 `EnergyLedger`。

- **【边界】鉴权形同虚设（B8）**：token 不校验、ownerId 可枚举；仅校方端点有 `_require_school`(v3_routes.py:40)。

### 2.3 核心流程闭环验证（3 条关键链路）

- **流程① 登录→测评→领宠→训练→结算→排行**：除 B7（测评成长不入库）外全链真实闭环 ✅
- **流程② 目标→任务包→工作台→成果→集市**：主体闭环、数据落库、可复用；但能量口径分裂（B1），守护活动两套解锁机制并存（B2/流程②）⚠️
- **流程③ 可疑判断→宠物成长**：仅在"已领宠"前提下成立；不写 EnergyLedger（B1）⚠️

### 2.4 补全建议

1. 所有学习行为统一经 `energy_service.award_energy`，择一作为"主成长"，三套口径合并。
2. seed 一个 `published` 示例主题，或在集市完成动作中也 `award_energy`，使 V3 贡献可成立。
3. 明确对外口径为"规则引擎 + 可选 LLM 增强"；让 `GrowthRule` 真正驱动 `training_growth` 或删除该面板。
4. 测评完成时 `apply_growth` 或与宠物创建解耦补发；风险判断成长与宠物顺序解耦。
5. 删除 `get_session_questions` 死代码；统一任务包为 `LearningPlan`，废弃 `TaskPackage` 表。

---

## 三、代码质量

### 3.1 当前问题（附代码位置）

- **【严重·疑似 Bug】`get_session_questions` 语义错位**（assessment_service.py:425-454，见维度二 B5）：返回全量题，与"分层随机"冲突（虽当前未被调用）。

- **【中等】死代码 / 死数据（确证无引用）**
  - `RiskTestSample` 220 条精标种子（`risk_test_samples.py`）**从不读取**——安全护栏与效果验证未消费这批数据，属"建了不用的死数据"。
  - `act_err.json`(21B)、`dash2.json`(6.9KB 缓存响应)、`=6.0`(1.8KB SSL 报错日志，误入仓库)、`fix_quotes.py`、`backend.log`(29KB)、`fraud_pet.db` 均为垃圾/运行时产物，违反制品 hygiene。

- **【中等】前端 N+1 / 全表扫描 / 非原子**
  - 后端 `counselor_dashboard`(main.py:~2074) 在 `for owner_id in ...[:50]` 循环内逐用户查 Pet（最多 50 次独立查询）。
  - `energy_service._latest_entry`(energy_service.py:35-40) 每次取余额全表 `order_by(desc).first()`；"读最新→算→插新行"两步无事务/锁，高并发下余额覆盖/丢失。

- **【严重】异步/同步混用（FastAPI 反模式）**
  `scenario_reply`(main.py:1758, `async def`) 调 `await AIService.dialogue(db,...)`，`db` 来自同步 `Session`；`await` 让出事件循环后继续用同一同步 Session 调 `log_ai_call`(ai_service.py:161)。同步 ORM 阻塞事件循环，且同步 Session 非线程安全，跨 `await` 复用有数据竞争（SQLite `check_same_thread=False` 只是掩盖报错）。

- **【中等】前端 `any` 泛滥**
  `client.ts` 内 `apiRequest<any>` 数十处（:860,866,…），所有 `transform*`(:205,:241,…) 入参 `any`，仅靠 `?? 0` 兜底。后端改字段结构编译期无感知，运行时才炸。

- **【中等】重复/死组件**
  三套 `StatCard`（`ui/StatCard.tsx`、`CounselorDashboardPage.tsx:213`、`EvidenceCenterPage.tsx:254`）；`Tooltip.tsx` 全仓零引用（死组件）；`client.ts` 非 v1 场景 API(:902-966)、`SESSION_KEY` 双定义(client.ts:140 与 useAppStore.ts:27)；`analyzeImage`(client.ts:815)/`uploadLearningArtifactFile`(client.ts:1260) 手写 fetch 绕过 `apiRequest`。

- **【中等】前端性能：整店订阅 + 缺 memo**
  7 处 `useAppStore()` 无选择器整店订阅（SettlementPage:14、PetPage:26、RankingPage:13…），任意 `set()` 触发重渲染；大列表 `RankingPage`/`KnowledgePage` 无虚拟化；`KnowledgePage`(792 行)、`LearningTaskPage`(833 行) 每次渲染重建大量内联对象。

- **【轻微】常量重复**：`ABILITY_DIMENSIONS` 等在 ability_profile.py 与 assessment_service.py 两处；`ACTIVITY_BOUNDARY_NOTICE` 重复 3 处。

### 3.2 改进方案

1. 实现 `get_session_questions` 过滤 `session.selected_ids` 或直接删除该函数。
2. 清理 `act_err.json`/`dash2.json`/`=6.0`/`backend.log`/`fraud_pet.db`，加 `.gitignore`；将 `RiskTestSample` 接入安全护栏评估或删除 seed。
3. 余额改为聚合 `func.sum` 实时计算或 `(owner_id, account_type)` 唯一约束 + 事务增量更新；dashboard 改 `IN` 查询/Join。
4. DB 操作统一 `AsyncSession`，或同步调用包进 `run_in_threadpool`；`AIService` 不在内部持有同步 Session。
5. 为后端响应定义 `Raw*` 类型 + zod 运行时校验后再映射；删除死组件/legacy API；`SESSION_KEY` 与 token 解析合并一处。
6. 用选择器 `useAppStore(s=>s.xxx)` 替代整店订阅，大列表引入虚拟化。

---

## 四、数据流与状态管理

### 4.1 当前问题（附代码位置）

- **【高】全局单一 `isLoading` 布尔 → 并发竞态**
  `useAppStore.ts` 的 `isLoading` 被 `register/login/adoptPet/submitTraining/startScenarioTraining/finishScenarioTraining` 等 10+ action 共享置位（:202,219,236,287…）。两动作并发时 A 完成 `set({isLoading:false})` 会提前关闭 B 的 loading，反之亦然。

- **【高】聊天回复不设 `isLoading` → 可重入 + 消息丢失**
  `replyScenarioTraining`(useAppStore.ts:463) 全程不设 `isLoading`；`ScenarioTrainingPage.tsx:77` 仅用 `isLoading` 防重入，回复在途时仍可狂点。且用 `get().scenarioSession` 读取当前消息再 append(:469-481)——两条并发回复以"发送前同一份 session"为基，`set` 相互覆盖，造成消息丢失/乱序。

- **【高】全局 `error` 单一字符串 → 跨页错误泄漏**
  `error` 被众多 action 写入，但 `clearError()` 全仓仅 `TaskPackagePage.tsx:64` 调用。`SuspiciousCheckPage.tsx:14`、`PetSelectPage.tsx:86` 直接渲染 `error`：A 页失败切到 B 页，陈旧错误仍显示。

- **【高】结算答案依赖 `location.state` → 刷新丢失 + 重复发奖**
  `SettlementPage.tsx:21-22` 答案来自 `location.state.answers`；刷新 → `location.state` 为 null → `answers=[]` → 渲染成"成长值 0"伪成功态(:66 仅当 `!result && !pet` 才报错)。`submitTraining` 防重只靠局部 `submittedRef`(:19)，**React 18 StrictMode 双调用 + 返回重进**会重建 ref → 重复发成长值/盾能。`LearningTaskPage.tsx:375` 的 setTimeout 自动完成同样有重入风险。

- **【高】数据获取层分裂（store vs 直连 API）**
  约 14 个页面直接 `import { api }` 绕过 store（TrainingSessionPage:25、PetPage:26、KnowledgePage:19…）。同一份"宠物/任务"数据在 store 与页面局部 state 两份存在，易不一致，store 缓存形同虚设。

- **【中】乐观更新冲突**
  `analyzeSuspicious`(useAppStore.ts:359) 先乐观保留 pet，再 `getMyPet` 覆盖；并发触发时旧 pet 覆盖新的。store 缺"请求序列号/取消"机制。

- **【中】前端路由守卫可被篡改绕过**
  `StudentLayout.tsx:44`/`SchoolLayout.tsx:26-32` 仅依据 localStorage 的 `currentUser/role`。攻击者可改 localStorage 中 `role='school'` 进入校方端（后端只信前端传入 ownerId/role 则构成越权）。前端守卫仅作体验优化，不可作安全边界。

- **【中】后端三套成长/能量口径分裂（见维度二 B1）**：数据流终点不一致，排行榜/V3 盾能对主演示路线无感知。

### 4.2 改进方案

1. **废弃 store 级 `error`**，各页用局部 `useState` 管理 loading/error（参照 `KnowledgePage` 已做对）；`isLoading` 改为 per-action 的 `loadingMap` 或局部态。
2. 结算答案/结果由 store 或后端按 `settlementId` 持久化；进入结算页先用 `settlementId` 拉取；前端 `sessionStorage` 防重复提交标记 + 后端幂等。
3. 聊天回复期间本地置 loading 锁；改用"基于上一层消息追加"的不可变更新，或后端返回完整消息列表后整体替换。
4. 收敛数据获取：页面只经 store action 取数，补齐缺失 action（`getKnowledge` 等），删除页面内 `import { api }`。
5. 明确"前端守卫仅体验优化"，所有敏感接口后端按 token 真实身份强鉴权；前端 `role` 以后端返回为准。

---

## 五、错误处理与健壮性

### 5.1 当前问题（附代码位置）

- **【中等】缺统一异常处理与边界校验**
  仅零星 `try/except`（LLM、图片分析）。`knowledge_analyze_image`(main.py:1124) catch 后返回 `success:False` 而非 4xx/5xx，前端难区分。自由文本缺 `max_length`：`RiskAnalyzeRequest.text`(main.py:155) 仅 `min_length=1`；`EmergencyStopLossRequest`/`ScenarioReplyRequest.message` 均无上限。

- **【严重】Admin 端点零鉴权 + 可写 GrowthRule**
  `GET/POST /api/admin/cases`(main.py:1172/1193)、`PUT /api/admin/rules`(main.py:1252) 无认证；`admin_save_rules` 接收 `dict[str,Any]` 无 key 白名单。关键矛盾：`GrowthRule` 表**从未参与计算**（成长上限用的是 `rules.py` 常量），即写了库却毫无效果（死功能 + 暴露写入面）。

- **【严重】`emergency_stop_loss` 污染模块级常量（并发安全）**
  `get_stop_loss_checklist`(emergency_stop_loss.py:88) 执行 `item["completed"]=False`，`item` 来自模块级常量 `STOP_LOSS_CHECKLIST`。多次调用/并发请求相互污染，单测不可重现。

- **【中等】`activity_service.contribute` 多步非事务**
  `contribute`(activity_service.py:68) 依次 `invest_energy`→写 `ActivityContribution`→更新 `CampusActivity` 进度，无单事务/乐观锁。并发贡献会出现重复扣盾能、进度计数不准。

- **【中等】`/api/risk/analyze-ai` 成长值发放不一致且可滥用**
  `risk_analyze_ai`(main.py:1927)：`growth_awarded = 10 if pet else 0` **每次调用无条件 +10**，无 `SUSPICIOUS_CHECK_DAILY_LIMIT` 限频、不写 `SuspiciousCheck` 记录（对比 `risk/analyze` 有限频与落库），且无鉴权，可对任意 ownerId 无限刷成长值。

- **【中等】大量 store action 静默吞错（前端）**
  `loadDashboard`(:397)、`loadAbilityProfile`(:406)、`loadEvidenceOverview`(:525)、`loadAILogs`(:534)、`loadDueRetrains`(:544)、`loadActiveTheme`(:567)、`loadEnergy`(:602)、`loadActivities`(:611)、`loadSchoolDashboard`(:635) 的 catch **只 `console.error`**，不设 `error` 也不设 `isLoading` → 首页/主题等失败即空白页无提示。

- **【中】后端原始错误直接透传 UI（前端）**
  `apiRequest`(client.ts:168-177) 在非 2xx 时把 `response.text()` 原样 `throw`，后端 500 返回的 stack/SQL/路径被 `showError(e.message)` 直接显示给用户（信息泄露 + 体验差）。

- **【中】前端重复提交 / 弱表单校验**
  结算、学习任务自动完成有重入发奖风险；`LearningTaskPage` 复盘/成果仅 `trim().length>=10`(:366,368) 弱校验；图片上传 `analyzeImage`(client.ts:815) 前端不校验类型/大小。

- **【轻微】测试覆盖极薄**
  `tests/test_api.py` 仅 4 个 happy-path；`client.test.ts` 仅 1 个用例（只测 demoLogin 成功）；service/安全/错误路径零测试。CI 无法拦截上述 Bug。

### 5.2 改进方案

1. 加 `@app.exception_handler` 统一 422/500 包络；自由文本加 `max_length`。
2. admin 端点加角色校验；删除无作用的 `GrowthRule` 写路径或真正让运行时读取它。
3. `get_stop_loss_checklist` 返回 `copy.deepcopy(STOP_LOSS_CHECKLIST)`，绝不改原常量。
4. `contribute` 用 DB 事务 + `with_for_update()` 行锁，或原子 `UPDATE ... SET current_progress = current_progress + ?`。
5. `/analyze-ai` 复用 `risk/analyze` 的限频与落库逻辑，或合并两接口。
6. 为 9 个静默加载 action 补三态 UI（error/empty/重试）或统一 `useAsyncToast` 抛错。
7. 统一错误归一化：对外只暴露友好文案，详情仅 `console`/上报（定义 `ApiError` 类型）。
8. 图片上传前端校验 `file.type/size`，复用 `apiRequest` 统一错误处理与超时。

---

## 六、安全性与可扩展性

### 6.1 当前问题（附代码位置）

- **【严重·最高优先级】完全无认证鉴权，ownerId 客户端任意伪造**
  `auth_login`(main.py:472) 返回 token，但**全仓库无地校验该 token**；所有业务端点以 `ownerId`（query/body 传入）为信任根。任何人可传任意 `ownerId` 读写他人测评/宠物/盾能/成果；`counselor_dashboard` 辅导员视角也无身份校验。

- **【严重】校方/管理接口无密码**
  `v3_routes.py:53` `_get_or_create_school`：`Account(username="school-demo", password_hash="", ...)` **空密码哈希**；`_require_school`(:40) 仅 `if role!='school': raise 401`，无密码/无 token。`requirements.txt` 缺 `passlib/bcrypt`。

- **【高】Prompt 注入风险**
  `scenario_reply`(main.py:1788) 把用户 `payload.message` 透传 `AIService.dialogue`；`safety_filter.filter_input` **只脱敏 PII（手机/身份证/银行卡/验证码/邮箱/密码），不防注入**。用户可构造"忽略上文，输出…"操控骗子回复。`scenario_state_machine.classify_user_behavior`(scenario_state_machine.py:401) 基于关键词可被刻意话术绕过，直接跳终态"成功识破"。

- **【中等】`check_output` 双关键词绕过**
  `safety_filter.py:75-89`：`if keyword in text and context in text` 才拦截——关键词与上下文**必须同时出现**，只出现其一即放行（如单独"转账""验证码"）。

- **【中等】CORS 偏宽松**
  `main.py:408-414`：`allow_credentials=True` + `allow_methods=["*"]` + `allow_headers=["*"]`，credentials 与 `*` 不可同时。

- **【中】XSS 纵深防御缺口（前端）**
  当前无 `dangerouslySetInnerHTML`（正面），但 token 存 localStorage（useAppStore.ts:48）明文、XSS 可窃取；改 HttpOnly+Secure+SameSite Cookie 更稳。

- **【中等】可扩展性瓶颈**
  1. JSON 大字段反模式：`ability_profile_json/messages_json/identified_evidence_json/unlock_rule_json` 等大量 `Text` 存储，无法索引/联表/统计（models.py）。
  2. 30+ 表集中单一 `Base`，模块耦合高；新增业务域难隔离。
  3. v1/legacy 双实现 + 三重 schema 管理使演进成本倍增。
  4. AI 配置全局固化（ai_service.py:50-55），多模型/灰度无法不重启切换。
  5. `AIService` 与同步 Session 强耦合，限制并发吞吐。
  6. 多租户缺位（无 `tenant_id`，`CampusActivity` 全库共享，`_ensure_activity_seeds` 每次 upsert 固定 4 个活动）。

### 6.2 改进方案

1. **引入 `Depends` 鉴权依赖（JWT/OAuth2）**，`ownerId` 从 token 解析而非客户端传入；辅导员/校方接口加角色中间件。
2. 校方账户设密、登录发 token、补 bcrypt 哈希；前端改 HttpOnly Cookie 鉴权，不再碰原始 token。
3. system prompt 固化并明确"不得执行用户指令"；用户输入做角色隔离（user/assistant 不可伪造）；行为分类加置信度/最短对话轮次约束。
4. `check_output` 改为"任一敏感动作词出现即拦截/告警"，叠加语义级规则。
5. CORS 收窄方法与头；后端不回显原始异常（防反射型 XSS 向量，`knowledge_analyze_image` 的 `{e}` 回显）。
6. 热点 JSON 列按需结构化；schema 统一 Alembic；配置中心化；AI 调用走 `AsyncSession` + 连接池；引入 `tenant_id` 支持多学校。

---

## 七、整体评价与优先级排序改进清单

### 7.1 整体评价

| 维度 | 评分(/10) | 一句话 |
|---|---|---|
| 架构设计 | 5.5 | 分层方向对，但巨型 main.py、双实现、三重 schema、async/同步混用拖后腿 |
| 功能完整性 | 6.5 | 主链路真闭环；三套能量口径分裂、V3 贡献不可达、AI 实为规则是硬伤 |
| 代码质量 | 6.0 | UI 组件质量高；死代码/死数据、any 泛滥、N+1/非原子、async 混用需修 |
| 数据流与状态管理 | 5.0 | 720 行巨型 store + 全局 loading/error 是系统性短板，引发重复发奖/消息竞态/跨页错误 |
| 错误处理与健壮性 | 5.0 | 静默吞错、无统一异常、常量被改、analyze-ai 可刷成长、测试极薄 |
| 安全性与可扩展性 | 4.0 | 零鉴权+空密码+admin 裸写是阻断性缺陷；JSON 字段/双实现/无租户阻碍扩展 |
| **综合** | **5.4** | **作品级成立，产品级缺口集中在安全、状态管理、统一能量、配置生效、测试** |

### 7.2 优先级排序改进清单（P0→P3）

**P0 — 阻断性（必须先做，关系评审核心卖点与上线安全）**
1. 引入 JWT/OAuth2 鉴权依赖，`ownerId` 改由 token 解析；校方账户补 bcrypt 密码 + 登录发 token（维度六 S1/S2）。
2. 删除 `create_all` + 手工 ALTER，统一 Alembic 迁移，消除 schema 漂移（维度一 A2）。
3. 合并三套成长/能量为单一 `energy_service.award_energy` 账本，使排行榜/V3 盾能对全路线感知（维度二 B1 / 维度四）。
4. 修复结算幂等：答案/结果按 `settlementId` 持久化 + 后端幂等 + 前端防重，杜绝重复发奖（维度四 4.2 / 维度五）。
5. 拆分/改造 720 行 store：废弃全局 `error`，`isLoading` 改为 per-action 或局部态，消除并发竞态与跨页错误泄漏（维度四）。

**P1 — 高（核心卖点真实性 & 主要健壮性）**
6. 让 `GrowthRule` 真正驱动成长计算，或删除后台面板避免误导（维度二 B6）。
7. seed 一个 `published` 示例主题，使 V3 盾能贡献/共建解锁可达（维度二 B2）。
8. 明确对外口径"规则引擎 + 可选 LLM 增强"；默认接入轻量模型体现 AI 原生（维度二 B3/B4）。
9. 修复 `emergency_stop_loss` 常量污染（deepcopy）、`/analyze-ai` 限频与落库（维度五 R3/R5）。
10. 统一错误归一化（友好文案 + 详情仅 console）、为 9 个静默加载补三态 UI（维度五）。
11. admin 端点加角色校验；`check_output` 改"任一敏感词即拦"（维度五/六）。
12. 前端数据获取收敛到 store，删除 14 处直连 API（维度四）。

**P2 — 中（质量与一致性）**
13. 抽取 `main.py` 路由到独立 `routes/` 模块，业务下沉 service（维度一 A1/A3）。
14. 清理死代码/垃圾文件（`act_err.json`/`dash2.json`/`=6.0`/`backend.log`/`fraud_pet.db`/`Tooltip`/legacy API），加 `.gitignore`（维度三）。
15. 删除 `get_session_questions` 死代码；统一任务包为 `LearningPlan`，废弃 `TaskPackage` 表（维度二）。
16. 余额聚合计算/事务原子；dashboard 改 IN 查询；消除 N+1（维度三）。
17. 为后端响应定义 `Raw*` + zod 校验，消除 `any` 泛滥；用选择器替代整店订阅（维度三）。
18. DB 统一 `AsyncSession` 或 `run_in_threadpool`，消除 async/同步混用（维度三 Q7）。

**P3 — 低（加固与扩展）**
19. 补齐 pytest / vitest 覆盖（service、安全、错误路径、transform 函数）（维度三/五）。
20. Prompt 注入加固（system 固化 + 角色隔离 + 行为置信约束）；CORS 收窄（维度六）。
21. 引入 `tenant_id` 支持多学校；热点 JSON 列结构化（维度六 D4/S7）。
22. 自由文本加 `max_length`；图片上传前端校验类型/大小（维度五）。

---

> 说明：本报告所有证据均来自对仓库实际源码的逐文件阅读，问题定位精确到文件与行号。建议按 P0→P3 顺序整改，先补齐安全基座与统一成长/能量体系，再消除重复与死代码，最后补测试锁住回归。如需，我可针对任一 P0 项直接产出可落地的重构代码（如 JWT 鉴权依赖、统一 Alembic 迁移、结算幂等改造）。
