# =============================================================================
# 诈醒学集 — 生产环境配置模板
# 复制本文件为 .env 并填写实际值：cp .env.example .env
# 注意：.env 含密钥，请勿提交到代码仓库（已在 .gitignore 忽略 *.env）
# =============================================================================

# -------------------------------------------------------------------------
# 后端监听
# -------------------------------------------------------------------------
# 后端进程端口（容器内）
BACKEND_PORT=8000

# -------------------------------------------------------------------------
# 数据库（生产使用 PostgreSQL，数据持久化于命名卷 postgres-data）
# -------------------------------------------------------------------------
POSTGRES_USER=fraud_pet
POSTGRES_PASSWORD=ReplaceMeWithStrongPassword
POSTGRES_DB=fraud_pet
# 后端使用的 SQLAlchemy 连接串（compose 已自动注入，通常无需改动）
DATABASE_URL=postgresql+psycopg2://fraud_pet:ReplaceMeWithStrongPassword@postgres:5432/fraud_pet

# -------------------------------------------------------------------------
# 鉴权与密钥
# -------------------------------------------------------------------------
# 生产环境必须为 true：关闭 /api/auth/demo-login 与 /api/school/demo-login
# 生成命令：python -c "import secrets; print(secrets.token_hex(32))"
APP_SECRET=ReplaceWithStrongRandomSecretAtLeast32Chars

# -------------------------------------------------------------------------
# 前端 API 地址
# -------------------------------------------------------------------------
# 生产（前后端同域，nginx 反代）：使用相对路径 /api
# 前后端分处不同域名时改为：https://your-domain.com/api
VITE_API_BASE_URL=/api

# -------------------------------------------------------------------------
# 功能开关
# -------------------------------------------------------------------------
# 是否启用"证据中心"页面（设为 true 开启）
VITE_ENABLE_EVIDENCE_CENTER=false

# -------------------------------------------------------------------------
# 体验账号（仅开发环境、配合 /login?demo=1 使用）
# 生产环境请务必留空，避免暴露演示账号
# -------------------------------------------------------------------------
VITE_DEMO_OWNER_ID=

# -------------------------------------------------------------------------
# 真实 AI 接入（可选；未配置时 AI 训练走规则引擎降级）
# 推荐低成本国产模型：deepseek-chat / qwen-turbo / glm-4-flash
# 也可指向兼容 OpenAI 协议的任意网关（含自建）
# -------------------------------------------------------------------------
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini
