# ==============================================================================
# 诈醒学集 — 前端 Nginx 配置（生产加固版）
# 静态文件服务 + API 反向代理到后端容器
# 已启用：gzip 压缩、基础安全响应头、SPA 路由回退、上传限流。
# HTTPS 见文件末尾注释模板（需自备证书后启用）。
# ==============================================================================

# 隐藏 Nginx 版本号，降低信息泄漏
server_tokens off;

# gzip 压缩：减小静态资源体积，提升首屏加载
gzip on;
gzip_comp_level 6;
gzip_min_length 1024;
gzip_proxied any;
gzip_vary on;
gzip_types
    text/plain
    text/css
    text/javascript
    application/javascript
    application/json
    application/xml
    image/svg+xml
    font/woff2;

server {
    listen       ${PORT};
    server_name  _;

    # 基础安全响应头
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Permissions-Policy "geolocation=(), microphone=(), camera=()" always;

    # 前端静态文件
    location / {
        root   /usr/share/nginx/html;
        index  index.html;
        # SPA 路由回退
        try_files $uri $uri/ /index.html;
    }

    # API 反向代理到后端
    location /api/ {
        proxy_pass http://backend:8000/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # 文件上传大小限制 (图片上传 10MB，预留余量)
        client_max_body_size 12m;
        # 代理超时保护
        proxy_connect_timeout 10s;
        proxy_send_timeout 30s;
        proxy_read_timeout 30s;
    }

    # 静态资源缓存（带内容哈希的文件可长期缓存）
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        root /usr/share/nginx/html;
        expires 7d;
        add_header Cache-Control "public, immutable";
        add_header X-Content-Type-Options "nosniff" always;
    }
}

# ==============================================================================
# HTTPS 模板（启用前准备）：
# 1. 将证书文件挂载到容器 /etc/nginx/certs/ 下（fullchain.pem / privkey.pem）。
# 2. 取消下方 server 块注释，并将上方 80 端口 server 块改为 301 跳转到 https。
# 3. 反向代理的 X-Forwarded-Proto 已随 scheme 自动传递，无需额外改动。
#
# server {
#     listen       443 ssl;
#     server_name  your-domain.com;
#
#     ssl_certificate     /etc/nginx/certs/fullchain.pem;
#     ssl_certificate_key /etc/nginx/certs/privkey.pem;
#     ssl_protocols       TLSv1.2 TLSv1.3;
#     ssl_ciphers         HIGH:!aNULL:!MD5;
#
#     # HSTS：强制浏览器仅通过 HTTPS 访问（启用证书后取消注释）
#     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
#     add_header X-Content-Type-Options "nosniff" always;
#     add_header X-Frame-Options "SAMEORIGIN" always;
#
#     location / {
#         root   /usr/share/nginx/html;
#         index  index.html;
#         try_files $uri $uri/ /index.html;
#     }
#
#     location /api/ {
#         proxy_pass http://backend:8000/api/;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         client_max_body_size 12m;
#     }
# }
# ==============================================================================
