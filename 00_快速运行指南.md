# 00 快速运行指南

本指南面向赛事组验收人员。按下面步骤操作，可以在新电脑上启动本项目主 Demo。

## 1. 安装基础环境

请先确认电脑已安装：

```powershell
python --version
node --version
npm.cmd --version
```

推荐版本：

- Python 3.11.x
- Node.js 20.19+、22.12+ 或 24+
- npm 10+

## 2. 启动后端 API

打开第一个 PowerShell 窗口，在参赛包根目录执行：

```powershell
cd .\04_run_scripts
powershell -ExecutionPolicy Bypass -File .\01_install_backend.ps1
powershell -ExecutionPolicy Bypass -File .\02_start_backend.ps1
```

看到类似内容即表示后端启动成功：

```text
Uvicorn running on http://127.0.0.1:8000
```

后端健康检查地址：

```text
http://127.0.0.1:8000/api/health
```

## 3. 启动前端页面

打开第二个 PowerShell 窗口，在参赛包根目录执行：

```powershell
cd .\04_run_scripts
powershell -ExecutionPolicy Bypass -File .\03_install_frontend.ps1
powershell -ExecutionPolicy Bypass -File .\04_start_frontend.ps1
```

看到类似内容即表示前端启动成功：

```text
Local: http://127.0.0.1:5173/
```

浏览器打开：

```text
http://127.0.0.1:5173
```

## 4. 登录信息

```text
主人 ID：U-2408**
密码：任意输入
```

点击“演示账号登录 / 进入系统”即可进入 Demo。

## 5. 手动命令方式

如不使用脚本，也可以手动执行。

后端：

```powershell
cd .\01_source_main_demo\fraud-pet-demo\backend
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install --upgrade pip
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
.\.venv\Scripts\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8000 --reload
```

前端：

```powershell
cd .\01_source_main_demo\fraud-pet-demo
npm.cmd ci
$env:VITE_API_BASE_URL="http://127.0.0.1:8000/api"
npm.cmd run dev -- --host 127.0.0.1 --port 5173
```

## 6. 验证命令

安装依赖后可运行：

```powershell
cd .\04_run_scripts
powershell -ExecutionPolicy Bypass -File .\05_run_checks.ps1
```

检查内容：

- FastAPI 后端接口测试
- 前端 API client 测试
- 前端生产构建

## 7. 常见问题

如果 PowerShell 提示 `npm.ps1 cannot be loaded`，请使用文档中的 `npm.cmd` 命令，或直接运行本包脚本。本包脚本已经使用 `npm.cmd`。

如果端口被占用：

- 后端默认端口：8000
- 前端默认端口：5173

可以结束占用端口的程序后重试。

如果前端页面打开但请求失败，请确认后端窗口仍在运行，并访问 `http://127.0.0.1:8000/api/health`。
